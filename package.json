{
  "name": "action-latest-release",
  "version": "1.0.0",
  "description": "Get latest release of repository",
  "main": "main.js",
  "scripts": {
    "package": "ncc build main.js -o dist",
    "build": "ncc build main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@octokit/rest": "^19.0.7",
    "semver-compare": "^1.0.0"
  }
}
